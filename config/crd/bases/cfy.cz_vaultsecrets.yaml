---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: vaultsecrets.cfy.cz
spec:
  group: cfy.cz
  names:
    kind: VaultSecret
    listKind: VaultSecretList
    plural: vaultsecrets
    singular: vaultsecret
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.secretName
      name: Secret
      type: string
    - jsonPath: .status.lastUpdated
      name: Last Updated
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.message
      name: Message
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: VaultSecret is the Schema for the vaultsecrets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VaultSecretSpec defines the desired state of VaultSecret.
            properties:
              immutable:
                default: false
                description: |-
                  Immutable, if set to true, ensures that data stored in the Secret cannot be
                  updated (only object metadata can be modified). If not set to true, the
                  field can be modified at any time. Defaulted to nil.
                type: boolean
              rolloutObjectsRef:
                description: list of other CRD object for rollout/restart purposes
                items:
                  properties:
                    apiVersion:
                      enum:
                      - apps/v1
                      type: string
                    kind:
                      enum:
                      - Deployment
                      - StatefulSet
                      - DaemonSet
                      type: string
                    name:
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - name
                  type: object
                type: array
              stringData:
                additionalProperties:
                  description: VaultSecretDataValue represents a value in StringData
                    with validation.
                  pattern: ^[^@]+@[^@]+$
                  type: string
                description: StringData is an example field of VaultSecret. Edit vaultsecret_types.go
                  to remove/update
                type: object
              type:
                default: Opaque
                description: |-
                  stringData allows specifying non-binary secret data in string form. It is
                  provided as a write-only input field for convenience. All keys and values
                  are merged into the data field on write, overwriting any existing values.
                  The stringData field is never output when reading from the API.
                type: string
            type: object
          status:
            description: VaultSecretStatus defines the observed state of VaultSecret.
            properties:
              lastUpdated:
                type: string
              message:
                type: string
              secretName:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
