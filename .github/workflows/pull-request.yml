name: Pull Request

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Dump github context (pretty)
        run: |
          echo '${{ toJson(github) }}' | jq .

  build-docker-container:
    uses: ./.github/workflows/_build_docker_container.yml

  #debug-outputs:
  #  runs-on: ubuntu-latest
  #  needs:
  #    - build-docker-container
  #  steps:
  #    - run: |
  #        echo ${{ needs.build-docker-container.outputs.artifact-id }}